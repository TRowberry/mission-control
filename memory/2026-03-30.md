# 2026-03-30 Session Log

## Wiki Documentation - Screenshots

### MC Polling Issue
- Tim noticed my MC responses were delayed
- Root cause: Only checking MC during heartbeat polls (~30 min intervals)
- Messages between polls sat unread until next poll
- Explained to Tim that webchat/Signal trigger immediately, MC doesn't

### QA Bot Credentials
- Old credentials from test scripts didn't work (qa@missioncontrol.local)
- Created new account: screenshots@mc.local / screenshot-bot-2026
- Saved creds in `memory/mc-screenshot-creds.json`

### Screenshots Captured
Using browser automation with openclaw profile:
1. Dashboard - overview with stats, quick actions, recent activity
2. Kanban - board view with columns
3. Chat - #general channel with messages
4. Settings - admin hub showing Agents, LLM Providers, QA Testing
5. Agents list - all agents with status, provider, usage
6. Agent dropdown - Edit/Duplicate/Run Now/View Logs/Delete options
7. Agent edit modal - Basic Info tab with username, role, webhook URL

Saved to: `C:\Users\Rico\.openclaw\media\browser\`

### Blocked
- Wiki pages are user/workspace-scoped
- Screenshot Bot account doesn't see existing wiki pages
- Need to either upload via API or Tim adds manually

## MC Webhook Fix (Afternoon)

### Problem
- @mentions in MC weren't triggering me in real-time
- MC was getting 401 Unauthorized when calling OpenClaw gateway

### Root Cause
- `OPENCLAW_GATEWAY_TOKEN` was hardcoded as `111111` in `docker-compose.yml`
- OpenClaw auth mode changed from password to token
- Actual token is `openclaw_relay_token_2026`

### Fix
1. Updated `~/apps/mission-control/docker-compose.yml` on GraySkull
2. Changed `OPENCLAW_GATEWAY_TOKEN: 111111` to `OPENCLAW_GATEWAY_TOKEN: openclaw_relay_token_2026`
3. Ran `docker compose down && docker compose up -d`

### Result
- MC @mentions now trigger real-time wake via `/tools/invoke` endpoint
- Stored fix to MC extended memory
